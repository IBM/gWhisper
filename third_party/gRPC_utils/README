Files in this directory were taken from the official gRPC repository (https://github.com/grpc/grpc).

They include the RPC reflection interface description, as well as
very useful gRPC RPC call and reflection abtractions.

Some minor modifications were made to those files to allow building them
in this projects (include paths). Those modifications are clearly marked.
