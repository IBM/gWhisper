language: cpp

os:
    - linux

dist: xenial

before_install:
    - sudo apt-get update
    - sudo apt-get install -y g++ cmake build-essential autoconf libtool pkg-config
    - ./travis/installGrpc.sh


script: ./build.sh
